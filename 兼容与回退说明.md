# 兼容与回退说明

## 旧角色兼容
- 旧角色登录后采用“懒初始化 + 默认值回填”。
- 以下字段不要求重建角色：
  - `origin/background`
  - `base/status/combat attributes`
  - `skills/spells/recipes`
  - `identity_track`
  - `contribution_state`
  - `reputation_state`
  - `unread_board_count`

## 前端回退策略
- 若 `StructuredPanel` 的新增字段缺失：
  - 仍按基础面板渲染
- 若 `GameEvent.channel/tone/render_mode` 缺失：
  - 客户端按默认日志样式显示
- 若 `SceneSnapshot.service_tags/rumor_topics/board_available/mentor_ids` 缺失：
  - 前端按空数组或 false 处理，不抛异常

## 世界包兼容原则
- `doc/design` 不直接参与运行时。
- 运行时只读 `doc/mud/world_data.json`。
- 新增结构源缺项时必须在构建期失败，不允许运行期静默缺失：
  - `help_topics`
  - `jobs`
  - `identity_tracks`
  - `rumor_sources`

## 事件兼容
- `board_post` 继续走现有事件流，不新增独立数据库表。
- 帖子隐藏状态继续落在玩家 `flags_json`，保证旧表结构仍可运行。

## 回退原则
- 若新主文本渲染字段不稳定，可回退到基础 `panel + line` 模式。
- 若某项新玩法影响主流程，可先保留数据源和帮助条目，暂时关闭命令入口。
