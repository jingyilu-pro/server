# 阶段验收定义表

## P0 总体验收
- 新号从注册、登录、建角进入世界后，不依赖高频弹窗也能完成：
  - 看场景
  - 问风声
  - 看工作
  - 看委托板
  - 发帖、读帖、收帖
  - 查看身份与排行
- 刷新页面或重新 `bootstrap` 后，角色、场景和主流程状态可恢复。
- 新手前 `10` 分钟能跑完第一轮：
  - `look`
  - `talk / ask`
  - `work`
  - `board`
  - `say`
- 新手前 `90` 分钟能完成首个共享世界闭环：
  - `help`
  - `rumor`
  - `work`
  - `say/tell/post`
  - `duty` 或散修推进

## 帮助系统
- 至少 `8` 个帮助主题。
- `help <topic>` 可直接输出字符板。
- 必须包含：
  - `newbie`
  - `newbie_10`
  - `newbie_90`
  - `economy`
- `commands` 至少包含：
  - 命令名
  - 别名
  - usage
  - 中文摘要

## 风声与公众任务
- 嘉元城、太南谷、天南港至少各有 `1` 处 rumor 源。
- `ask <npc> about rumor` 返回：
  - 正文
  - 相关事务或任务
  - 后续可继续动作

## 工作系统
- `work` 至少在 `7` 个关键场景有内容。
- 每项工作都要有：
  - 发起人
  - 地点
  - 奖励摘要
  - 指令提示
- 四条身份路线上至少都有自己不止 `1` 条的可重复事务：
  - 七玄门
  - 散修
  - 黄枫谷
  - 灵兽山

## 共享世界密度
- 至少 `60` 个场景具备以下任意 `2` 类以上：
  - 服务标签
  - 风声主题
  - 导师
  - 板面
  - 资源点
- 七玄门起步区、官道驿棚、嘉元与太南外圈、宗门内圈、天南港与群岛、虚天殿桥接区都必须有连续可玩的节点串。

## 留言板
- 有板子的房间必须支持：
  - `board`
  - `read`
  - `post`
  - `discard`
- `discard` 只影响当前玩家。
- 刷新和重登后，帖子仍可被再次读到。
- 双账号验证时：
  - A 发帖后 B 可见
  - A `discard` 后只对 A 隐藏
  - B 仍能读到

## 身份线
- 散修 / 七玄门 / 黄枫谷 / 灵兽山都可查看：
  - 当前身份
  - 下一阶门槛
  - 推荐事务入口

## 协议与兼容
- 新增 proto 字段至少有一处服务端写入和前端消费。
- 新字段缺失时，前端回退到基础字符板渲染，不白屏。

## 自动化
- 必跑命令：
  - `node scripts/build_mud_world.mjs`
  - `npm --prefix client test -- --run`
  - `npm --prefix client run build`
  - `wsl bash -lc "cd /mnt/c/Work/Projects/server && cmake --build build-wsl-main --target mud_smoke -j10 && ./build-wsl-main/scripts/mud_smoke"`
